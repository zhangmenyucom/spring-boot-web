<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.taylor.dao.ParticipantDao">

    <resultMap type="com.taylor.entity.ParticipantEntity" id="participantMap">
        <result property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="lotteryId" column="lottery_id"/>
        <result property="tickets" column="tickets"/>
        <result property="status" column="status"/>
        <result property="address" column="address"/>
        <result property="phone" column="phone"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

	<select id="queryObject" resultType="com.taylor.entity.ParticipantEntity">
		select
			`id`,
			`user_id`,
			`lottery_id`,
			`tickets`,
			`status`,
			`address`,
			`phone`,
			`create_time`,
			`update_time`
		from participant
		where id = #{id}
	</select>

	<select id="queryList" resultType="com.taylor.entity.ParticipantEntity">
		select
    		`id`,
    		`user_id`,
    		`lottery_id`,
    		`tickets`,
    		`status`,
    		`address`,
    		`phone`,
    		`create_time`,
    		`update_time`
		from participant
		WHERE 1=1
        <if test="merchantId!=null">
            and merchant_id=#{merchantId}
        </if>
		<if test="name != null and name.trim() != ''">
			AND name LIKE concat('%',#{name},'%')
		</if>
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
			<otherwise>
                order by id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from participant
		WHERE 1=1
        <if test="merchantId!=null">
            and merchant_id=#{merchantId}
        </if>
        <if test="name != null and name.trim() != ''">
            AND name LIKE concat('%',#{name},'%')
        </if>
	</select>
	 
	<insert id="save" parameterType="com.taylor.entity.ParticipantEntity" useGeneratedKeys="true" keyProperty="id">
		insert into participant(
			`user_id`,
			`lottery_id`,
			`tickets`,
			`status`,
			`address`,
			`phone`,
			`create_time`,
			`update_time`)
		values(
			#{userId},
			#{lotteryId},
			#{tickets},
			#{status},
			#{address},
			#{phone},
			#{createTime},
			#{updateTime})
	</insert>
	 
	<update id="update" parameterType="com.taylor.entity.ParticipantEntity">
		update participant 
		<set>
			<if test="userId != null">`user_id` = #{userId}, </if>
			<if test="lotteryId != null">`lottery_id` = #{lotteryId}, </if>
			<if test="tickets != null">`tickets` = #{tickets}, </if>
			<if test="status != null">`status` = #{status}, </if>
			<if test="address != null">`address` = #{address}, </if>
			<if test="phone != null">`phone` = #{phone}, </if>
			<if test="createTime != null">`create_time` = #{createTime}, </if>
			<if test="updateTime != null">`update_time` = #{updateTime}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete from participant where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from participant where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>